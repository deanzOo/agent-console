# Copy to .deploy.env and fill in. Gitignored: your server is yours.
DEPLOY_HOST=my-server
DEPLOY_PATH=/opt/agent-console

# Optional. When set, the deploy is not considered finished until this answers,
# which is what catches a container that reports healthy and still cannot serve.
DEPLOY_URL=https://console.example.com
